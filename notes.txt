
docker images - list local images.
docker ps - list running containers


Ran this to build:
docker build . -t blerb-api:1

Run the image above in a container
docker run -it --rm -p 8080:8080 blerb-api:1

To run with docker-compose
docker-compose up -d --build


To do:

Research docker & docker-compose commands

====================================================================================

Troubleshooting.

Networking...

--> Check to see if maybe it's just a naming issue?
    :: See what the convention for referencing api from ui in docker-compose.

--> Figure out what the network (or whatever) fields do in docker compose.

--> Find an online tutorial with a front and backend connected.

--> Watch or rewatch some tutorials.